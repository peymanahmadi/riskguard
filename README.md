# riskguard

A pluggable, concurrency-safe Go library for detecting fraud and abuse in
payment transactions in real time — velocity limits, impossible-travel
detection, amount thresholds, new-device checks, and blacklists, combined
into a single risk score and decision (`approve` / `review` / `decline`).